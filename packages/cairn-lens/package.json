{
  "name": "@isaacriehm/cairn-lens",
  "displayName": "Harness Lens",
  "description": "Inline citation context for §V invariants and TODO(TSK-) tokens. Hovers, gutter icons, code lens — resolved from .harness/ground/ ledgers.",
  "version": "0.1.0",
  "publisher": "isaacriehm",
  "type": "module",
  "engines": {
    "node": ">=22",
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.cjs",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": {
      "title": "Harness Lens",
      "properties": {
        "harness.lens.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Master switch for Harness Lens features."
        },
        "harness.lens.inlineDecorations": {
          "type": "boolean",
          "default": true,
          "description": "Render ghost text after §V<N> tokens with the resolved invariant title."
        },
        "harness.lens.gutterIcons": {
          "type": "boolean",
          "default": true,
          "description": "Show citation health icons in the gutter (active / superseded / not found)."
        },
        "harness.lens.codeLens": {
          "type": "boolean",
          "default": true,
          "description": "Render a Code Lens line above functions whose file matches a decision's scope_globs."
        },
        "harness.lens.decExplorer": {
          "type": "boolean",
          "default": false,
          "description": "Show the DEC Explorer sidebar tree view."
        }
      }
    },
    "views": {
      "explorer": [
        {
          "id": "harnessLens.decExplorer",
          "name": "Harness DEC Explorer",
          "when": "config.harness.lens.decExplorer && config.harness.lens.enabled"
        }
      ]
    }
  },
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/isaacriehm/cairn.git"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo *.vsix",
    "bundle": "esbuild src/extension.ts --bundle --platform=node --format=cjs --target=node22 --outfile=dist/extension.cjs --external:vscode --external:fsevents",
    "package": "pnpm clean && pnpm bundle && vsce package --no-dependencies --out isaacriehm-cairn-lens-0.0.0.vsix",
    "smoke:resolver": "tsx scripts/smoke-resolver.ts"
  },
  "dependencies": {
    "@isaacriehm/cairn-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.6.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
