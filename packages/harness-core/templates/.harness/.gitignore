# Per-session state — owned by exactly one Claude Code session.
# Created at SessionStart, removed at SessionEnd, GC'd if stale.
sessions/

# Per-write flock + whole-operation locks (per PLUGIN_ARCHITECTURE §7).
.write-lock
.gc-lock
.audit-lock
