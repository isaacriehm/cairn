# Per-session state — owned by exactly one Claude Code session.
# Created at SessionStart, removed at SessionEnd, GC'd if stale.
sessions/

# Per-clone run logs — agent event streams, rendered prompts, diffs,
# sensor results, UAT artifacts under runs/active|terminal/<run-id>/.
# Local execution record, not shared state; heavy + per-clone. Marked
# GITIGNORED in docs/FILESYSTEM_LAYOUT.md.
runs/

# Invalidation event log — inter-session signal, 7-day retention,
# regenerable. Spec: PLUGIN_ARCHITECTURE §7.
events/

# Per-write flock + whole-operation locks (per PLUGIN_ARCHITECTURE §7).
.write-lock
.gc-lock
.audit-lock

# Per-clone attested-commits log — written by post-commit hook, read by
# Stop hook bypass detection. Per PLUGIN_ARCHITECTURE §17 Layer 1.
.attested-commits

# Per-clone CLI invocation path — written by `cairn join`, read by the
# git hooks to resolve the bundled cli.mjs or fall back to a global cairn.
.cli-path

# Haiku response cache — keyed on tier|system|prompt|jsonSchema; 30-day
# TTL. Lets re-runs of `cairn init` skip identical Haiku calls without
# burning the operator's coding-plan quota. Regenerable.
cache/

# Mid-init crash-recovery state. Phase tools persist here between
# invocations so an interrupted `cairn init` can resume; also a
# multi-MB serialized blob on real-world adoptions. Once init is
# done it's stale, fully regenerable from a re-init.
init-state.json

# Phase output spillover — mapper-output.json (full mapper result) and
# source-comments-walk.json (full Phase 7b walk + classifications).
# Kept on disk so MCP responses stay skinny; both regenerable.
init/

# Per-machine MCP telemetry — staleness sensor inputs, mcp-calls.jsonl.
# Local debug only; never useful in version control.
staleness/

# Pre-strip recovery copies — `.cairn/backups/source/<rel>.original`
# snapshots created by Phase 7b INV strip + DEC strip-replace. Per-
# clone safety net for `git checkout` recovery; teammates have their
# own snapshots from their own adoption pass.
backups/

# Task state — per-developer work-in-flight (active spec, journal,
# attestation; done archive). Tasks are local to whoever is driving
# them; teammates see graduated knowledge via the DEC/§INV ground
# layer, not via raw task dirs. Untrack via `cairn fix gitignore`
# on projects adopted before v0.11.3.
tasks/

# Mission state — per-developer roadmap progress (cursor, phase
# progress, journal). Same reasoning as tasks/: missions are local
# planning artifacts; shared outputs land in ground/. Untrack via
# `cairn fix gitignore` on projects adopted before v0.11.3.
missions/

# Draft decision inbox — operator-pending DEC review queue. Drafts
# don't graduate to ground until accepted; per-developer attention
# queue. Already excluded from canonical-zone reads in spec §2.2.
ground/decisions/_inbox/

# Derived ground-state indexes + caches. Regenerated from the committed
# DEC/INV `.md` sources (which carry sot_kind / sot_path /
# sot_content_hash frontmatter) by `cairn join` + SessionStart via
# `rebuildDerived`. Committing them caused multi-dev merge conflicts —
# every clone rewrote them locally (timestamps, content hashes, token
# caches) and pushed divergent copies. Now per-clone. Untrack on repos
# adopted before v0.15.0 via `cairn fix gitignore`.
ground/manifest.yaml
ground/scope-index.yaml
ground/quality-grades.yaml
ground/topic-index.yaml
ground/anchor-map.yaml
ground/sot-cache.yaml
ground/sot-bindings.yaml
ground/file-candidates-map.yaml
ground/decisions/decisions.ledger.yaml
ground/invariants/invariants.ledger.yaml

# Per-clone alignment-pending queue — operator-pending SoT-drift review
# items (idempotent content-slug filenames), resolved via
# cairn_resolve_attention. Same class as ground/decisions/_inbox/: drafts
# don't graduate to shared ground until accepted.
ground/alignment-pending/

# Per-clone GC marker — last-run timestamp for the Stop-hook GC
# auto-trigger. Written locally by the hook, never shared.
.gc-last-run

# Per-clone stall-warning dedup markers — `.stalled-warned/<task-id>.iso`
# records that the Stop hook already surfaced a stall cue for a task this
# session, so it isn't re-fired. Tied to per-clone tasks/; never shared.
.stalled-warned/

# Per-clone phase-defer marker — sets a 24h suppression on the
# mission phase-exit prompt. Local to whoever picked "Defer 24h"
# in the cairn-attention surface.
.mission-phase-deferred-until

# Per-clone hook telemetry — `state/telemetry/hooks.jsonl` records
# every plugin hook invocation locally for debug; not shareable.
state/telemetry/

# Per-clone align/undo log — `cairn attention undo` reads it to reverse
# this clone's Layer A auto-resolutions. Append-only JSONL, meaningful
# only on the clone that performed the aligns; committing it conflicts on
# concurrent appends across devs and can't undo a teammate's work.
state/align-undo-log.jsonl

# Per-clone fix-align dry-run preview — transient state written by
# `cairn fix align --dry-run`, regenerated on demand. Never shareable.
state/fix-align-dryrun.json

# Per-clone sensor baseline snapshots — `cairn init` phase 11 +
# `cairn baseline` rerun write `sensor-audit-<ISO>.yaml` here.
# Regenerable from current source; not version-controlled.
baseline/
