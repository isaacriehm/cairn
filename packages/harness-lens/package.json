{
  "name": "@isaacriehm/harness-lens",
  "displayName": "Cairn Lens",
  "description": "Inline citation context for §V invariants and TODO(TSK-) tokens. Hovers, gutter icons, code lens — resolved from .harness/ground/ ledgers.",
  "version": "0.0.0",
  "publisher": "isaacriehm",
  "type": "module",
  "engines": {
    "node": ">=22",
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": {
      "title": "Cairn Lens",
      "properties": {
        "harness.lens.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Master switch for Cairn Lens features."
        },
        "harness.lens.inlineDecorations": {
          "type": "boolean",
          "default": true,
          "description": "Render ghost text after §V<N> tokens with the resolved invariant title."
        },
        "harness.lens.gutterIcons": {
          "type": "boolean",
          "default": true,
          "description": "Show citation health icons in the gutter (active / superseded / not found)."
        },
        "harness.lens.codeLens": {
          "type": "boolean",
          "default": true,
          "description": "Render a Code Lens line above functions whose file matches a decision's scope_globs."
        },
        "harness.lens.decExplorer": {
          "type": "boolean",
          "default": false,
          "description": "Show the DEC Explorer sidebar tree view."
        }
      }
    },
    "views": {
      "explorer": [
        {
          "id": "harnessLens.decExplorer",
          "name": "Cairn DEC Explorer",
          "when": "config.harness.lens.decExplorer && config.harness.lens.enabled"
        }
      ]
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "smoke:resolver": "tsx scripts/smoke-resolver.ts"
  },
  "dependencies": {
    "@isaacriehm/harness-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/vscode": "^1.85.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
