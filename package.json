{
  "name": "harness-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Portable agent harness for solo developers (root monorepo).",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
